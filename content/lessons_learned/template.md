# 经验沉淀模板

复制到 `lessons_learned/` 新文件或追加 README。

```markdown
### YYYY-MM-DD · 标题

- **场景**：
- **现象**：
- **根因**：
- **解法**：
- **规则**：（给 AI 的一句话）
- **标签**：
```

## 何时写
- 验收失败 → 修复通过
- 审查发现 Critical
- Prompt 改版后明显更稳

## 与 Cursor Rules 关系
累积 10+ 条后，可提炼为 `.cursor/rules` 里的永久家规。
